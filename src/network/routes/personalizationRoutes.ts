import { API_BASE_URL } from '../config';

const personalizationRoutes = {
  personalization: {
    events: `${API_BASE_URL}/api/v1/feed-personalization/events/`,
  },
};

export default personalizationRoutes;
