import type { KISPalette } from '@/theme/constants';

type Props = {
  palette?: KISPalette | Record<string, any>;
  kind?: string;
  title?: string;
  subtitle?: string;
  label?: string;
  icon?: string;
};

export default function EnterpriseKcanRevenuePreviewCard(_props: Props) {
  return null;
}
