// Jest manual mock for @react-native-community/netinfo — see the
// async-storage mock in this same __mocks__ tree for why this re-exports
// the package's own official mock instead of a hand-rolled one.
module.exports = require('@react-native-community/netinfo/jest/netinfo-mock');
