# ─── macOS ───────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
Thumbs.db

# ─── Xcode ───────────────────────────────────────────────────────────────────
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcuserstate
**/.xcode.env.local
# Pods are regenerated by `pod install` — don't commit them
**/Pods/
ios/build/
ios/build-ipad/

# ─── Android ─────────────────────────────────────────────────────────────────
.idea/
.gradle/
local.properties
*.iml
*.hprof
.cxx/
.kotlin/
android/build/
android/app/build/
# Keep debug.keystore (needed for local dev builds)
*.keystore
!debug.keystore

# ─── Node.js ─────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log
yarn-error.log
# Local node_modules patch staging directory
node_modules_updated_files/

# ─── React Native ────────────────────────────────────────────────────────────
*.jsbundle
# Metro file-watcher health checks
.metro-health-check*
# pnpm local store (not part of the project)
.pnpm-store/

# ─── Environment & secrets ───────────────────────────────────────────────────
.env
.env.*
!.env.example

# ─── Ruby / Bundler / CocoaPods ──────────────────────────────────────────────
/vendor/bundle/
.bundle/

# ─── Fastlane ────────────────────────────────────────────────────────────────
**/fastlane/report.xml
**/fastlane/Preview.html
**/fastlane/screenshots
**/fastlane/test_output

# ─── Testing / Coverage ──────────────────────────────────────────────────────
/coverage

# ─── Yarn ────────────────────────────────────────────────────────────────────
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ─── Claude Code ─────────────────────────────────────────────────────────────
# Worktrees are temporary git branches Claude creates for isolated work.
# CLAUDE.md, settings.json, and any other .claude files ARE committed
# (they configure Claude's behaviour for this repo and are useful to share).
.claude/worktrees/

# ─── Git artifacts ───────────────────────────────────────────────────────────
*.orig
*.rej
