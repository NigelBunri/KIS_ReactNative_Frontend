// /Users/nigel/dev/KIS/node_modules/react-native-set-app-icon/android/build.gradle

apply plugin: "com.android.library"

def safeExtGet(prop, fallback) {
    return rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

android {
    // Use your app’s compileSdk / minSdk / targetSdk if they exist on rootProject,
    // otherwise fall back to reasonable defaults.
    compileSdkVersion safeExtGet("compileSdkVersion", 34)

    defaultConfig {
        minSdkVersion safeExtGet("minSdkVersion", 21)
        targetSdkVersion safeExtGet("targetSdkVersion", 34)
        consumerProguardFiles "consumer-rules.pro"
    }

    sourceSets {
        main {
            java.srcDirs = ["src/main/java"]
            manifest.srcFile "src/main/AndroidManifest.xml"
            res.srcDirs = ["src/main/res"]
        }
    }
}

repositories {
    mavenCentral()
    google()
}

dependencies {
    // Use whatever React Native version your app uses
    implementation "com.facebook.react:react-native:+"
}
